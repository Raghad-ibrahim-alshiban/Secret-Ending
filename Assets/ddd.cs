using UnityEngine;

public class ddd : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("ddd Running");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
